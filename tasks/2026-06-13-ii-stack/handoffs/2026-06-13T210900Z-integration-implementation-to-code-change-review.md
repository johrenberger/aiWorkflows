# Handoff packet

> Generated by the `handoff-packet` skill.

- **Packet timestamp (UTC):** 2026-06-13T210900Z
- **Source agent:** software-engineer
- **Target agent:** code-change-review
- **Approval required:** no

## 1. Task ID

2026-06-13-ii-stack

## 2. Source agent

software-engineer (using the `integration-implementation` skill)

## 3. Target agent

code-change-review

## 4. Objective

Review the new Node/Express proxy for correctness, security,
and operational fit. Then produce a code-change-review report.

## 5. Context summary

This is the first calibration exercise for the
`integration-implementation` skill, which has been at
`draft` for the entire session. The exercise produced UC1
(no skill baseline, 5 tests, 50 lines) and UC2 (with
skill, 17 tests, 211 lines), all passing.

The proxy calls the re-baselined
`johrenberger/spring-petclinic-rest` fork as a downstream.
The fork is a local dev upstream (not a real production
endpoint), so calling it from tests is allowed per the
rest-api profile.

The full implementation report is at:
`reports/integration-implementation-report.md`

## 6. Files read

- `/data/.openclaw/workspace/test-repo/skills/integration-implementation/SKILL.md`
- `/data/.openclaw/workspace/test-repo/skills/integration-implementation/references/profiles/rest-api.md`
- `/data/.openclaw/workspace/test-repo/skills/integration-implementation/templates/integration-implementation-report.md`
- `src/server.js` (the proxy)
- `tests/uc1.test.js` and `tests/uc2.test.js`

## 7. Files changed

- `package.json` (NEW) — declares express, axios, jest, supertest
- `src/server.js` (NEW) — proxy with retry, timeout, correlation, logging
- `tests/uc1.test.js` (NEW) — 5 tests for date-range proxy
- `tests/uc2.test.js` (NEW) — 17 tests for POST + retry logic

## 8. Commands run

- `npm install` (scaffolding, BEFORE the skill invocation)
- `npm test` — exit 0 — 22/22 pass, 1.3 sec

## 9. Validation results

- **Passed:** `npm test` (22/22 tests, 1.3 sec)
- **Skipped:** none
- **Failed:** none

## 10. Decisions made

- **`D1`**: Use `axios.request` (not `axios({...})`) so
  unit tests can mock with `jest.spyOn(axios, 'request')`.
  This is a small but real production-grade change
  motivated by testability.
- **`D2`**: Log only the 7 fields (ts, level, event,
  correlationId, status, attempt, latencyMs). Never log
  `description` (free text), `petId`, or any user-supplied
  field.
- **`D3`**: Generate a correlation id if the client didn't
  provide one. Pass it downstream and return it in the
  response header.
- **`D4`**: Propagate `Idempotency-Key` header on POST
  (the current upstream doesn't honor it, but it's plumbed
  for when a real upstream does).

## 11. Risks

- **`R1`**: The `Idempotency-Key` header is propagated but
  the current upstream doesn't honor it. If two retries
  reach the upstream, two visits are created. Owner:
  future work; mitigation: when the upstream is updated
  to honor the header, the proxy is already correct.
- **`R2`**: The proxy uses `axios` directly without a
  client wrapper. If the codebase grows, an `HttpClient`
  wrapper is recommended. Owner: future work.
- **`R3`**: Tests use a real local upstream; if the
  upstream is not running, the integration tests fail.
  Owner: developer; mitigation: the upstream is started
  in `docker-compose.yml` (or in this case, manually via
  `./mvnw spring-boot:run`).

## 12. Blockers

`none`

## 13. Required next action

Run `code-change-review` on the new files. Produce
`reports/code-change-review-report.md`.

Focus on:
1. Is the retry classification (`isRetryable`) correct for
   the failure modes documented in the implementation
   report?
2. Should the proxy be hardened against a malicious
   downstream response (e.g. extremely large JSON)?
3. Is the `Idempotency-Key` propagation the right pattern,
   or should the proxy generate its own?

## 14. Approval required

no — the source agent retains ownership until the
code-change-review report is produced.

## Acceptance

(To be filled in by the code-change-review agent.)
