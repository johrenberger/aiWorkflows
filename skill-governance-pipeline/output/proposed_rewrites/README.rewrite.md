---
name: README
artifact_type: unknown
purpose: |
  Tighten this 1-2 sentence purpose: who uses this skill,
  what it does, and in what context.
category: TBD
owner: TBD
version: "0.2.0"
inputs:
  - name: input_data
    type: file
    description: Describe the expected input shape.
outputs:
  format: json
  fields:
    - result
    - evidence
dependencies: []
intended_consumers: []
quality_level: draft
last_reviewed: 2026-06-13
---

# README (rewrite proposal)

> This file was generated by the `rewrite_generator` module. 
It is a *proposal*, not an automatic replacement. Review the 
changes, then either accept them or refine the rewrite.

## Why this rewrite was triggered

- `roi-decision-rewrite`

## What changed

1. **Metadata** is now a complete YAML frontmatter block with all required fields.
2. **Inputs** are declared as a list of named fields (not free-text).
3. **Outputs** declare an explicit `format` and a `fields` list.
4. **Boundaries** are explicit: this skill does one thing, in one context, for one consumer.
5. **Validation expectations** are concrete (see below).

## Token efficiency

- Original estimated tokens: 12
- Rewrite target: keep under 2,000 tokens for a single-purpose skill.
- If you need more, this is a sign that the skill is over-broad (consider splitting).

## Validation expectations

- `skill-governance validate` should pass with no blocking findings.
- The metadata parser should extract all 12 required fields.
- The contract validator should not flag vague outputs.
- If a benchmark exists for this skill, the runner should report `passed=true`.

## Original excerpt

```
# Fixture
[bad](does-not-exist.md)
[good](self.md)

```

## Compatibility and migration

- The original artifact was `README` (type: `unknown`).
- This rewrite preserves the original's intent but tightens scope.
- Consumers of the original should not see a behavioral change in
  the happy path; only the metadata contract is now stricter.
- To migrate:
  1. Replace the original file with this rewrite.
  2. Add the missing metadata fields to your local copy.
  3. Re-run `skill-governance validate` to confirm CI passes.

