# ADR 0002: Use versioned JSON certificates

Status: accepted. Use JSON objects validated against immutable schemas for early interoperability; a later JSONL batch format can retain the single-record contract.
