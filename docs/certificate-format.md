# Certificate format

`schemas/descent-certificate-v1.json` specifies a finite record with start, steps, and target. Consumers must recompute the trajectory before accepting semantic interpretation. Schema versions are immutable.
