# Experiment protocol

Each experiment directory records parameters, code revision, command, inputs, and output digest. Store large transient outputs outside Git under `results/`. Experiments guide conjecture selection; they never replace formal verification.
